interface:
  display_name: "CACP"
  short_description: "Repo-specific patterns and workflows for CACP"
  default_prompt: "Use the CACP repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true